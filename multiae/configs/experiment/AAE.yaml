# @package _global_

defaults:
  - override /model: base.yaml

# all parameters below will be merged with parameters from default configurations set above
# this allows you to overwrite only specified parameters
# default parameters for AAE model
model_name: "AAE"

model:
  variational: False
  eps: 1e-15
  joint_representation: False
  wasserstein: False
  sparse: False

discriminator:
  _target_: multiae.models.layers.Discriminator

  hidden_layer_dims: []
  bias: True
  non_linear: False
  output_dim: 1
  dropout: 0
  wasserstein: False

out_dir: ${original_work_dir}/${model_name}/${now:%Y_%m_%d}
