# @package _global_

model:
  sparse: False

encoder:
  default:
    enc_dist:
      _target_: multiae.base.distributions.MultivariateNormal

decoder:
  default:
    dec_dist:
      _target_: multiae.base.distributions.MultivariateNormal

prior:
  _target_: multiae.base.distributions.MultivariateNormal
  loc: 0
  scale: 1
