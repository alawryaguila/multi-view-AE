# @package _global_

model:
  sparse: True

encoder:
  enc_dist:
    _target_: multiae.base.distributions.MultivariateNormal

decoder:
  enc_dist:
    _target_: multiae.base.distributions.MultivariateNormal
