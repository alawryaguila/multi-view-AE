# @package _global_

model:
  use_GPU: True
  seed: 1000

  z_dim: 12
  private: True
  sparse: False

encoder:
  default:
    non_linear: True

decoder:
  default:
    non_linear: True
    init_logvar: -4
